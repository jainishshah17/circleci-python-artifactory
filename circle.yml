machine:
  python:
    version: pypy-2.2.1
dependencies:
  pre:
    - ./circleci-credential.sh
deployment:
  master:
    branch: master
    commands:
    - python setup.py register -r circleci-pypi
    - python setup.py sdist upload  -r circleci-pypi
    - pip install -r requirements.txt
